export enum EPlatformName { 
  android = 'android',
  ios = 'ios'
}

export enum EListLanguage {
  ru = 'ru',
  en = 'en',
}

export class Pages {
  static readonly login = 'Login'
}

export const baseUrl = ''